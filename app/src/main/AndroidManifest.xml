<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ugr.fajardo.prueba00">

    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:screenOrientation="portrait"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivityNuevo"
            android:label="@string/title_activity_main_nuevo"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".CuestionLevel1Activity" />
        <activity android:name=".FinalActivity" />
        <activity android:name=".AcercaDeActivity" />
        <activity android:name=".OpcionesActivity"></activity>
    </application>

</manifest>